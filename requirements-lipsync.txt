# uruvagam — local lip-sync (Phase B) dependencies.
# These resolve cleanly together. Install in TWO steps (idempotent):
#     pip install -r requirements-lipsync.txt
#     pip install --no-deps -r requirements-lipsync-nodeps.txt
#
# Why two files: lipsync 0.1.0 pins av>=14,<15, but we need av 17 (PyAV 14 does
# not build against the system FFmpeg). Keeping lipsync in its own --no-deps file
# preserves av 17. `pip check` will report that one intentional mismatch.

av==17.0.1
torch==2.11.0
opencv-python==4.13.0.92
face-alignment==1.5.0
librosa==0.10.2.post1
